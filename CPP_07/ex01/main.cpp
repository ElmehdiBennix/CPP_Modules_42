#include "iter.hpp"

#define SIZE 5

int main(void)
{
    size_t      size = 0;

    char str[SIZE] = {};
    size = sizeof(str) / sizeof(str[0]);

    iter(&str)

    int num[SIZE] = {};
    size = sizeof(num) / sizeof(num[0]);

    double str[SIZE] = {};
    size = sizeof(str) / sizeof(str[0]);

    std::string str[SIZE] = {};
    size = sizeof(str) / sizeof(str[0]);


}